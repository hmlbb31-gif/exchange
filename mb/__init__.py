"""Ядро MangaBuff авто-обменника.

Модули пакета:
- client   — HTTP-клиент MangaBuff (сессия, прокси, детект «протух логин»)
- parser   — разбор HTML страниц обменов
- decision — движок принятия решений по обмену (fail-closed правила)
- market   — трекер рынка и опорные цены
- cards    — база рангов карт + провайдер CardInfo
- trader   — оркестрация одной итерации обмена
- worker   — цикл опроса и фоновые задачи
- storage  — SQLite-хранилище (обмены, аккаунты, куки)
- cookies  — загрузка cookie из .env/cookies.txt
- models   — датаклассы предметной области
- history  — разбор истории обменов
- farm     — задачи фарма (используются worker/тестами)
"""
